shift + Print
	maimpick

super + Return
	alacritty

super + d
    j4-dmenu-desktop

super + {_,shift + }q
    bspc node -{c,k}

super + {_,shift + }{1,9,0}
    bspc {desktop -f,node -d} '^{1-9,10}' --follow
